import type { Component } from 'ripple';

export component Card(props: { $children: Component, $class: string }) {
	<div $class={`bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm ${props.$class}`}>
        <props.$children />
    </div>
}